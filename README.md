# TanStack Router + Clerk React SDK

## Setup .env.local

Copy the .env.example provided and configure with your Clerk credentials

```
VITE_CLERK_PUBLISHABLE_KEY
```

## Run the app

To run this example:

- `npm install` or `yarn`
- `npm start` or `yarn start`
````
# tanstack-router-clerk
